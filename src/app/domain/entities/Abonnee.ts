import {Address} from "./Address";

export class Abonnee {
  constructor(
    id: String,
    name: String,
    firstName: String,
    telephones: Array<number>,
    address: Address,
    siteId: String,
    reservoirId: String
  ) {
  }
}
